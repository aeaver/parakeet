[project]
name = "modmanager"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyinstaller>=6.18.0",
    "requests>=2.32.5",
]

[project.scripts]
hello = "pyinstaller --onefile --icon=parakeet.ico --noconsole --name=Parakeet main.py"

[dependency-groups]
dev = []
